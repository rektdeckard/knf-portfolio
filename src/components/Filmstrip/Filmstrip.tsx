import React from "react";
import Content from "../Content/Content";
import Header from "../Header/Header";

const Filmstrip: React.FC<{}> = () => {
  return (
    <Content>
      <Header title="Filmstrip" />
    </Content>
  );
};

export default Filmstrip;
